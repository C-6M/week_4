# RAG原理题

---

**Chunk（切块）：** 切分长文本，每一段独立检索，提高检索效率，方便计算。

**Embed（向量化）：** 对切块后的文本进行数学处理，每段文本转成一组数字（向量），标识其语义关联性，相近的向量距离也近。

**Retrieve（检索）：** 用问题的向量去向量库里找距离最近的 top-k 段落。

**Generate（生成）：** 大模型根据检索到的语料和问题进行总结输出，生成带依据的回答。
