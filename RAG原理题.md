# RAG原理题

用自己的话解释RAG四步骤，每步一句话。

---

**Chunk（切块）：** 把长文档切成小段落，每段是独立的检索单元。

**Embed（向量化）：** 把每段文本转成一组数字（向量），语义相近的向量距离也近。

**Retrieve（检索）：** 用问题的向量去向量库里找距离最近的 top-k 段落。

**Generate（生成）：** 把检索到的段落和问题一起喂给 LLM，生成带依据的回答。
